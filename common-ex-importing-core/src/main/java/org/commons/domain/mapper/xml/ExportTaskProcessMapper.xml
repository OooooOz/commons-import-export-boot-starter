<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.commons.domain.mapper.ExportTaskProcessMapper">

    <resultMap id="ExportTaskProcessResultMap" type="org.commons.domain.model.entity.ExportTaskProcess">
        <id property="id" column="id"/>
        <result property="businessSystem" column="business_system"/>
        <result property="businessType" column="business_type"/>
        <result property="taskNo" column="task_no"/>
        <result property="taskName" column="task_name"/>
        <result property="status" column="status"/>
        <result property="fileName" column="file_name"/>
        <result property="fileUrl" column="file_url"/>
        <result property="message" column="message"/>
        <result property="startTime" column="start_time"/>
        <result property="endTime" column="end_time"/>
        <result property="creator" column="creator"/>
    </resultMap>

    <sql id="Base_Column_List">
        id,
        business_system,
        business_type,
        task_no,
        task_name,
        status,
        file_name,
        file_url,
        message,
        start_time,
        end_time,
        creator
    </sql>

    <select id="selectTaskPage" resultMap="ExportTaskProcessResultMap">
        SELECT
        <include refid="Base_Column_List"/>
        FROM export_task_process
        <where>
            <if test="query != null and query.businessSystem != null and query.businessSystem != ''">
                AND business_system = #{query.businessSystem}
            </if>
            <if test="query != null and query.businessType != null and query.businessType != ''">
                AND business_type = #{query.businessType}
            </if>
            <if test="query != null and query.creator != null and query.creator != ''">
                AND creator = #{query.creator}
            </if>
            <if test="query != null and query.status != null">
                AND status = #{query.status}
            </if>
        </where>
        <choose>
            <when test="query != null and query.orderByClause != null and query.orderByClause != ''">
                ORDER BY ${query.orderByClause}
            </when>
            <otherwise>
                ORDER BY id DESC
            </otherwise>
        </choose>
    </select>

</mapper>
